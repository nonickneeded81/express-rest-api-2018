{
  "name": "express-rest-api-2018",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "cp -r src dist/src && cp -r config dist/config",
    "start:dev": "NODE_ENV=development && yarn db:migrate && nodemon src/index.js",
    "start:stg": "NODE_ENV=staging && yarn build && yarn db:migrate && node dist/src/index.js",
    "start:prod": "NODE_ENV=production && yarn build && yarn db:migrate && node dist/src/index.js",
    "docker:build": "docker-compose build",
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "docker:stg": "docker-compose -f docker-compose.yml -f docker-compose.stg.yml up",
    "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml up",
    "db": "sequelize --config config/db.cjs",
    "db:migrate": "yarn db db:migrate",
    "test": "yarn test:lint && yarn test:unit",
    "test:unit": "yarn mocha 'src/**/*.spec.js'",
    "test:unit:target": "yarn mocha",
    "test:lint": "eslint ./src/",
    "mocha": "NODE_ENV=test mocha --timeout 20000 --exit"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-jsx-a11y": "^6.8.0",
    "eslint-plugin-react": "^7.33.0",
    "mocha": "^10.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "express-validator": "^7.2.0",
    "lodash": "^4.17.21",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.0",
    "sequelize": "^6.37.0",
    "sequelize-cli": "^6.6.0"
  }
}
